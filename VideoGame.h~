#pragma once

#include <iostream>
#include <cstring>

namespace V
{
  class VideoGame
  {
  public:
    VideoGame(char, int, int);
    //char getTitle();
    //int getYear();
    char getPublisher();
    int getRating();
    int getType();
  private:
    //char title[80];
    //int year;
    char publisher;
    int rating;
    int type;
  };
}
